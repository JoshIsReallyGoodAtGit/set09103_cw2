filePath = the full path to the file 
childFolder = /profile/ or /post/
filename = the file name
fileRename = profile-pic.jpg
fileCheck = profile-pic


save the file 
then rename it
then check every file in that folder
if this file matches the new file  
      grab the file extension of the old file